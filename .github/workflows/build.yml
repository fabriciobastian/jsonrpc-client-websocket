name: 📦 Build

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@3
    - name: Use Node.js
      uses: actions/setup-node@3
      with:
        node-ersion: 18.16.0
    - name: Install and build
      run: |
        npm ci
        npm run build
    - name: Test
      run: npm run test
